# Implementation Plans

Plans 002-007 were generated by the improve skill on 2026-08-30 (audit at commit `4c6fd8c`, X-T5 focus); plan 001 predates them and is DONE. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Make camera capabilities and wire codecs firmware-scoped | P1 | L | — | DONE — exact profiles own all simulation/RAW write codecs |
| 002 | Sanitize untrusted PTP device strings in preflight errors | P1 | S | — | DONE — merged to main as `6b7166d`, full gate verified |
| 003 | Bound device-property array allocation by available bytes | P1 | S | — | DONE — merged to main as `7ddb02f`, full gate verified |
| 004 | Guarantee USB interface release on open failure | P2 | S | — | DONE — merged to main as `804fd12`, full gate verified |
| 005 | Latch Ctrl-C around irreversible camera writes | P2 | M | — | DONE — merged to main as `6a45f30`, full gate verified; manual X-T5 Ctrl-C smoke check remains a maintainer step |
| 006 | Cover every RAF validator rejection branch with tests | P2 | S | — | DONE — merged to main as `3783a61`, full gate verified |
| 007 | Build the gated 0xD18C namespace probe tooling (spike) | P3 | L | — | BLOCKED on device work — Steps 1-2 merged to main (`b409be9`: design note + gated refusal-only skeleton, verified). Both design blockers now resolved by maintainer (2026-08-30): raw single-property primitive sanctioned probe-scoped under a double feature gate; observable resolved capture-first (X RAW Studio USB capture) with the LCD probe as fallback. Next: plan 008 specifies the sanctioned primitive + revised Step 3; the capture and probe run against an X-T5 remain maintainer steps. |
| 008 | Add the sanctioned probe-scoped raw property write primitive + wire the probe | P3 | L | 007 | DONE — commit `9904e07` (executor worktree `agent-ad20a907695d9bdea`), full gate verified; sealed-surface `code-reviewer` pass returned APPROVE (0 critical/high). Awaiting merge by maintainer |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Plan 008 depends on plan 007's merged skeleton (`b409be9`): it replaces the
  probe's refusal with the guard sequence and adds the sanctioned primitive.
  Executable now without hardware (surface + gate + guard-sequence logic are
  fully unit-testable); the X RAW Studio capture and any physical probe run
  remain maintainer steps, so plan 007 stays BLOCKED on device work even after
  008 lands.

- Plans 002-006 are mutually independent and can execute in any order; the listed order is recommended leverage order.
- Plan 007 is tooling-only: it must NOT touch `fml/`, `tests/xt5_simulation_domain.rs`, or `docs/users/support.md`, and the physical-device probe run stays a maintainer step after the tooling lands.
- Plan 001 contains an internal sequence: characterization tests and evidence inventory must land before the schema and runtime dispatch migration.
- Do not add production support for any X-T5 firmware other than the already allowlisted 4.31 until the required physical-device capture set exists.

## Audited but not planned (2026-08-30 audit)

Findings confirmed in the audit but deliberately left without plans in this batch; re-plan on request:

- Distinct exit code for "camera state unknown, do not retry" vs generic failure (all non-clap errors currently exit 1) — composes well with plan 005.
- Write-direction PTP bulk chunk promotion (read side adapts since `e283866`; `chunk_policy.write.effective_bytes` never grows) — MED-risk perf win on 128 MiB restores.
- Fake-transport seam for `preflight::run()` composition testing (`Ptp` hard-codes `rusb::DeviceHandle`) — highest-value test gap, L-effort architectural change.
- `usb::get_camera(...)` boilerplate duplicated across 10 CLI call sites — S-effort helper extraction.
- Informational `cargo-llvm-cov` coverage job in CI — S-effort DX addition.
- Zero unit tests in `crates/codegen/src/cli/render.rs` collection/dedup logic — S-effort fixture tests.
- Probe audit log records only `outcome: "attempted"`, written before the write
  sequence runs (`crates/fujicli-dev/src/probe.rs`) — durable pre-write trail by
  design, but the log alone cannot distinguish "camera restored" from "left
  mutated after a failure" (the real outcome only reaches stderr). Surfaced by
  the plan 008 sealed-surface review as MEDIUM; fix is a second terminal JSONL
  line within the existing field allowlist. S-effort follow-up.
- Pre-existing `Vec::with_capacity(count)` in `descriptor.rs` Enumeration-form branch (line ~120, `u16` count, ~65,535-element ceiling) — same allocate-before-validate pattern as plan 003 at a much smaller scale; surfaced by the 003 executor, S-effort mirror of the 003 fix.
- Direction options not selected this round: non-destructive `backup verify` command (core exists as `verify_post_restore_export`); running-diff mode for `support/monitor-rendering.sh` captures.

## Findings considered and rejected

- Rely only on `GetDevicePropDesc`: rejected because it can constrain scalar PTP properties but cannot describe the internal RAW conversion profile field count, order, profile code, padding, or per-field logical mappings.
- Treat the latest known firmware profile as compatible with any newer version: rejected because it silently widens write support without wire evidence.
- Keep global canonical wire values and add per-firmware read aliases: rejected because the current first-value-wins serializer cannot choose a different canonical write value for different firmware.
- Raw-indexing helpers in `features/backup/artifact.rs:301-325`: verified every call site is preceded by an explicit length check; no reachable panic, not worth a plan.
- `data.get(offset..offset + 2)` without `checked_add` in `features/render/jpeg.rs:37`: input is bounded far below `usize` overflow; not exploitable.
- `build.rs` `cue export` shell-out: fixed local dev-time tool with fixed args — a supply-chain trust assumption, not a code vulnerability.
