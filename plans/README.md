# Implementation Plans

Generated by the improve skill on 2026-08-30. Execute the plan below as one
coherent migration. Read it fully before starting: it intentionally breaks the
current model-wide wire-codec contract instead of adding a compatibility shim.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Make camera capabilities and wire codecs firmware-scoped | P1 | L | — | DONE — exact profiles own all simulation/RAW write codecs |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- Plan 001 contains an internal sequence: characterization tests and evidence
  inventory must land before the schema and runtime dispatch migration.
- Do not add production support for any X-T5 firmware other than the already
  allowlisted 4.31 until the required physical-device capture set exists.

## Findings considered and rejected

- Rely only on `GetDevicePropDesc`: rejected because it can constrain scalar
  PTP properties but cannot describe the internal RAW conversion profile field
  count, order, profile code, padding, or per-field logical mappings.
- Treat the latest known firmware profile as compatible with any newer version:
  rejected because it silently widens write support without wire evidence.
- Keep global canonical wire values and add per-firmware read aliases: rejected
  because the current first-value-wins serializer cannot choose a different
  canonical write value for different firmware.
